import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'heartStonelib',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
